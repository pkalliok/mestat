description: Implement searches for points with specific tags or namespaces (users who marked them).
comment: maybe also points by date of addition.
comment: maybe also distance restriction.
comment: some thoughts on dynamic WHERE: https://github.com/krisajenkins/yesql/issues/13
commit: SQL queries for restrictions implemented, needs default maxdist
commit: implementing default maxdist, some tests are now failing
commit: maxdist param for search API, tests modified and pass, new defaults
